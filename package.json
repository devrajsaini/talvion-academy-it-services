{
  "name": "talvion-academy-it-services",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host --port 5173",
    "build": "vite build && node copy_dist_assets.cjs",
    "preview": "vite preview --host --port 5173"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
